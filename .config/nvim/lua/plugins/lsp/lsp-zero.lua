return {
  -- config = function ()
  --
  --   require('mason').setup({})
  --   require('mason-lspconfig').setup({
  --     ensure_installed = {},
  --     automatic_install = true,
  --     handlers = {
  --       lsp_zero.default_setup,
  --     },
  --   })
  -- end,
}
